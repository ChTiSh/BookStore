import React from "react"
const Loading: React.FC = () =>{
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <div>Hang on... loading ...</div>
    )
}
export default Loading;